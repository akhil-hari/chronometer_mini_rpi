create table schedules(
    schedule_id varchar(255) PRIMARY KEY,
    year integer,
    month integer,
    day integer,
    hour integer,
    weekday integer,
    hour integer,
    minute integer,

);